export type TSearchBarState = {
  inputValue: string;
};

export type THeaderProps = {
  title: string;
};

export type TPeople = {
  id: number;
  name: string;
  profession: string;
  company?: string;
  age: number;
  instagram?: string;
  facebook?: string;
  twitter?: string;
  photo: string;
};
